void main() {
  var names = <String>['asiap', 'oke', 'mantap'];

  for (var value in names) {
    print(value);
  }

  var nameSet = <String>{'asiap', 'oke', 'mantap'};
  for (var value in nameSet) {
    print(value);
  }
}